export const EXPECTED_PERMISSIONS_BLOCK = [
  "  permissions:",
  "    actions: write",
  "    checks: write",
  "    contents: read",
  "    discussions: write",
  "    pull-requests: write",
  "    statuses: write",
  "    deployments: read",
  "",
].join("\n");
