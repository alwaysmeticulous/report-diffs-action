export const EXPECTED_PERMISSIONS_BLOCK = [
  "  permissions:",
  "    contents: read",
  "    statuses: write",
  "    pull-requests: write",
  "    packages: read",
  "    deployments: read",
  "",
].join("\n");
